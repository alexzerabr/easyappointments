<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production: Block cleartext traffic (HTTPS only) -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug builds: Allow ALL cleartext traffic for development -->
    <debug-overrides>
        <base-config cleartextTrafficPermitted="true">
            <trust-anchors>
                <certificates src="system" />
            </trust-anchors>
        </base-config>
    </debug-overrides>
</network-security-config>
