# Easy!Appointments Development Environment Configuration
# Copie este arquivo para .env-dev e ajuste os valores conforme necessário
# NUNCA commite o arquivo .env-dev (ele está no .gitignore)

# =============================================================================
# APLICAÇÃO
# =============================================================================
APP_ENV=development
APP_DEBUG=true
APP_URL=http://localhost
APP_LANGUAGE=english
APP_TIMEZONE=America/Sao_Paulo

# =============================================================================
# BANCO DE DADOS
# =============================================================================
# Estas são credenciais de DESENVOLVIMENTO - altere conforme necessário
DB_CONNECTION=mysql
DB_HOST=easyappointments-dev-db
DB_PORT=3306
MYSQL_DATABASE=easyappointments
MYSQL_USER=user
MYSQL_PASSWORD=password
MYSQL_ROOT_PASSWORD=secret

# Diretório de dados do MySQL no host (será criado automaticamente)
MYSQL_DATA_DIR=./docker/mysql-dev

# =============================================================================
# PORTAS DE REDE
# =============================================================================
# Portas expostas no host para acessar os serviços
HTTP_PORT=8080
MYSQL_PORT=3306
MAILPIT_WEB_PORT=8025
MAILPIT_SMTP_PORT=1025

# =============================================================================
# MAILPIT (Servidor de Email para Desenvolvimento)
# =============================================================================
MAIL_HOST=easyappointments-dev-mailpit
MAIL_PORT=1025
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=
MAIL_FROM_ADDRESS=noreply@easyappointments.local
MAIL_FROM_NAME=EasyAppointments

# =============================================================================
# WHATSAPP INTEGRATION
# =============================================================================
# Chave de encriptação para tokens do WhatsApp
# Gere uma nova chave com: openssl rand -base64 32
# IMPORTANTE: Defina esta variável mesmo em dev para evitar erros
WA_TOKEN_ENC_KEY=dev_key_change_me_in_production_only

# Timeouts do WPPConnect (em segundos)
WPPCONNECT_TIMEOUT=30
WPPCONNECT_CONNECT_TIMEOUT=10

# Janela de execução de rotinas WhatsApp (minutos)
# Define quanto tempo no passado o sistema buscará envios de rotina perdidos
# Recomendado: 10-15 minutos para lidar com atrasos do worker
WHATSAPP_ROUTINE_WINDOW_MINUTES=10

# =============================================================================
# LOGGING E DEBUG
# =============================================================================
LOG_LEVEL=debug

# =============================================================================
# NOTAS
# =============================================================================
# 1. Este arquivo é para DESENVOLVIMENTO apenas
# 2. As credenciais aqui são inseguras e apenas para uso local
# 3. Para produção, use .env.production com credenciais fortes
# 4. O arquivo .env-dev está no .gitignore e não será commitado
# 5. Acesse a aplicação em: http://localhost:8080/index.php/installation
# 6. Acesse o Mailpit em: http://localhost:8025

